Find all services:
    $ dns-sd -B _services._dns-sd._udp local

I can fake a target by copying it's Bonjour announce to another system and replay it there again.
Works also if no ehash transmitted

Identity retrieved from phash. (image to known phashes).
phash stands for "Picture Hash". Only announced if _booleanValueForPreference
ehash stands for "Apple ID Email Hash". Associated with apple Id but not mandatory


Bei BonjourPublisher::publishInternal auf rdi==rbx+0x28 liegt ein pointer zu CFNetServiceRef für AirDrop


defaults write com.apple.NetworkBrowser DisableTLS 1

